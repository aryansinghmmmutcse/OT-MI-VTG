cg_detr/postprocessing_cg_detr.py:5:from standalone_eval.eval import eval_submission
cg_detr/postprocessing_cg_detr.py:10:    def __init__(self, clip_length=2, min_ts_val=0, max_ts_val=150,
cg_detr/postprocessing_cg_detr.py:11:                 min_w_l=2, max_w_l=70, move_window_method="center",
cg_detr/postprocessing_cg_detr.py:14:        self.min_ts_val = min_ts_val
cg_detr/postprocessing_cg_detr.py:16:        self.min_w_l = min_w_l
cg_detr/postprocessing_cg_detr.py:21:            clip_ts=self.clip_min_max_timestamps,
cg_detr/postprocessing_cg_detr.py:39:    def clip_min_max_timestamps(self, windows):
cg_detr/postprocessing_cg_detr.py:42:        ensure timestamps for all windows is within [min_val, max_val], clip is out of boundaries.
cg_detr/postprocessing_cg_detr.py:44:        return torch.clamp(windows, min=self.min_ts_val, max=self.max_ts_val)
cg_detr/postprocessing_cg_detr.py:56:        ensure the final window duration are within [self.min_w_l, self.max_w_l]
cg_detr/postprocessing_cg_detr.py:59:        small_rows = window_lengths < self.min_w_l
cg_detr/postprocessing_cg_detr.py:62:                windows, small_rows, self.min_w_l, move_method=self.move_window_method)
cg_detr/train.py:28:                    level=logging.INFO)
cg_detr/train.py:39:def train_epoch(model, criterion, train_loader, optimizer, opt, epoch_i, tb_writer):
cg_detr/train.py:40:    logger.info(f"[Epoch {epoch_i+1}]")
cg_detr/train.py:69:        optimizer.zero_grad()
cg_detr/train.py:73:        optimizer.step()
cg_detr/train.py:85:    tb_writer.add_scalar("Train/lr", float(optimizer.param_groups[0]["lr"]), epoch_i+1)
cg_detr/train.py:96:    logger.info("Epoch time stats:")
cg_detr/train.py:98:        d = {k: f"{getattr(meter, k):.4f}" for k in ["max", "min", "avg"]}
cg_detr/train.py:99:        logger.info(f"{name} ==> {d}")
cg_detr/train.py:102:def train(model, criterion, optimizer, lr_scheduler, train_dataset, val_dataset, opt):
cg_detr/train.py:104:        logger.info("CUDA enabled.")
cg_detr/train.py:129:    save_submission_filename = "latest_{}_{}_preds.jsonl".format(opt.dset_name, opt.eval_split_name)
cg_detr/train.py:132:            train_epoch(model, criterion, train_loader, optimizer, opt, epoch_i, tb_writer)
cg_detr/train.py:138:                    eval_epoch(model, val_dataset, opt, save_submission_filename, epoch_i, criterion, tb_writer)
cg_detr/train.py:149:            logger.info("metrics_no_nms {}".format(pprint.pformat(metrics_no_nms["brief"], indent=4)))
cg_detr/train.py:151:                logger.info("metrics_nms {}".format(pprint.pformat(metrics_nms["brief"], indent=4)))
cg_detr/train.py:169:                    "optimizer": optimizer.state_dict(),
cg_detr/train.py:179:                logger.info("The checkpoint file has been updated.")
cg_detr/train.py:185:                    logger.info(f"\n>>>>> Early stop at epoch {epoch_i}  {prev_best_score}\n")
cg_detr/train.py:191:                "optimizer": optimizer.state_dict(),
cg_detr/train.py:202:        #         "optimizer": optimizer.state_dict(),
cg_detr/train.py:215:def train_hl(model, criterion, optimizer, lr_scheduler, train_dataset, val_dataset, opt):
cg_detr/train.py:217:        logger.info("CUDA enabled.")
cg_detr/train.py:242:    save_submission_filename = "latest_{}_{}_preds.jsonl".format(opt.dset_name, opt.eval_split_name)
cg_detr/train.py:245:            train_epoch(model, criterion, train_loader, optimizer, opt, epoch_i, tb_writer)
cg_detr/train.py:251:                    eval_epoch(model, val_dataset, opt, save_submission_filename, epoch_i, criterion, tb_writer)
cg_detr/train.py:262:            logger.info("metrics_no_nms {}".format(pprint.pformat(metrics_no_nms["brief"], indent=4)))
cg_detr/train.py:264:                logger.info("metrics_nms {}".format(pprint.pformat(metrics_nms["brief"], indent=4)))
cg_detr/train.py:278:                    "optimizer": optimizer.state_dict(),
cg_detr/train.py:288:                logger.info("The checkpoint file has been updated.")
cg_detr/train.py:294:                    logger.info(f"\n>>>>> Early stop at epoch {epoch_i}  {prev_best_score}\n")
cg_detr/train.py:300:                "optimizer": optimizer.state_dict(),
cg_detr/train.py:311:                "optimizer": optimizer.state_dict(),
cg_detr/train.py:326:    logger.info("Setup config, data and model...")
cg_detr/train.py:329:    if opt.debug:  # keep the model run deterministically
cg_detr/train.py:333:        cudnn.deterministic = True
cg_detr/train.py:368:    model, criterion, optimizer, lr_scheduler = setup_model(opt)
cg_detr/train.py:369:    logger.info(f"Model {model}")
cg_detr/train.py:371:    logger.info("Start Training...")
cg_detr/train.py:375:        train_hl(model, criterion, optimizer, lr_scheduler, train_dataset, eval_dataset, opt)
cg_detr/train.py:377:        train(model, criterion, optimizer, lr_scheduler, train_dataset, eval_dataset, opt)
cg_detr/train.py:391:        logger.info("\n\n\nFINISHED TRAINING!!!")
cg_detr/train.py:392:        logger.info("Evaluating model at {}".format(best_ckpt_path))
cg_detr/train.py:393:        logger.info("Input args {}".format(sys.argv[1:]))
cg_detr/matcher.py:6:from scipy.optimize import linear_sum_assignment
cg_detr/matcher.py:55:                len(index_i) = len(index_j) = min(num_queries, num_target_spans)
cg_detr/matcher.py:67:        # The 1 is a constant that doesn't change the matching, it can be omitted.
cg_detr/scripts/charades_sta/train.sh:40:num_dummies=45
cg_detr/scripts/charades_sta/train.sh:69:--contrastive_align_loss_coef 0.002 \
cg_detr/scripts/charades_sta/train.sh:78:--num_dummies ${num_dummies} \
cg_detr/scripts/charades_sta/train_vgg.sh:44:num_dummies=45
cg_detr/scripts/charades_sta/train_vgg.sh:65:--contrastive_align_loss_coef 0.002 \
cg_detr/scripts/charades_sta/train_vgg.sh:76:--num_dummies ${num_dummies} \
cg_detr/scripts/youtube_uni/train.sh:41:for num_dummies in 1
cg_detr/scripts/youtube_uni/train.sh:77:                --num_dummies ${num_dummies} \
cg_detr/scripts/tacos/train.sh:50:num_dummies=50
cg_detr/scripts/tacos/train.sh:72:--contrastive_align_loss_coef 0.002 \
cg_detr/scripts/tacos/train.sh:81:--num_dummies ${num_dummies} \
cg_detr/scripts/tvsum/train_tvsum.sh:41:        for num_dummies in 3
cg_detr/scripts/tvsum/train_tvsum.sh:71:                --num_dummies ${num_dummies} \
cg_detr/start_end_dataset.py:26:        'train': ['kLxoNp-UchI', 'NyBmCxDoHJU', 'jcoYJXDG9sw', '-esJrBWj2d8'],
cg_detr/start_end_dataset.py:42:        'train': ['GsAD1KT1xo8', 'XkqCExn6_Us', 'b626MiF1ew4', 'PJrm840pAUI'],
cg_detr/start_end_dataset.py:46:        'train': ['RBCABdttQmI', 'z_6gVvQb2d0', '4wU_LUjG5Ic', '91IHQYk1IQM'],
cg_detr/start_end_dataset.py:150:            logger.info("Using {}% of the data: {} examples"
cg_detr/start_end_dataset.py:229:        gt_ed = max(0, min(int(gt_window[1] / clip_len), ctx_l) - 1)
cg_detr/start_end_dataset.py:256:        maximum scores as positive, and two with the minimum scores as negative.
cg_detr/start_end_dataset.py:270:        # the min(_, ctx_l-1) here is incorrect, but should not cause
cg_detr/start_end_dataset.py:272:        hard_pos_clip_indices = [min(rel_clip_ids[idx], ctx_l-1) for idx in sort_indices[-max_n:]]
cg_detr/start_end_dataset.py:273:        hard_neg_clip_indices = [min(rel_clip_ids[idx], ctx_l-1) for idx in sort_indices[:max_n]]
cg_detr/start_end_dataset.py:291:        maximum scores as positive, and two with the minimum scores as negative.
cg_detr/start_end_dataset.py:304:        # score_array = [min(agg_scores[idx], ctx_l-1) for idx in range(ctx_l)]
cg_detr/start_end_dataset.py:314:        # the min(_, ctx_l-1) here is incorrect, but should not cause
cg_detr/start_end_dataset.py:316:        hard_pos_clip_indices = [min(rel_clip_ids[idx], ctx_l-1) for idx in sort_indices[-max_n:]]
cg_detr/start_end_dataset.py:317:        hard_neg_clip_indices = [min(rel_clip_ids[idx], ctx_l-1) for idx in sort_indices[:max_n]]
cg_detr/start_end_dataset.py:335:        agg_scores = np.sum(labels - np.ones_like(labels), axis=-1)[:ctx_l] # start from 1, so minus 1
cg_detr/start_end_dataset.py:339:        hard_pos_clip_indices = [min(idx, ctx_l-1) for idx in sort_indices[-max_n:]]
cg_detr/start_end_dataset.py:340:        hard_neg_clip_indices = [min(idx, ctx_l-1) for idx in sort_indices[:max_n]]
cg_detr/start_end_dataset.py:365:        hard_pos_clip_indices = [min(idx, ctx_l-1) for idx in sort_indices[-max_n:]]
cg_detr/start_end_dataset.py:366:        hard_neg_clip_indices = [min(idx, ctx_l-1) for idx in sort_indices[:max_n]]
cg_detr/start_end_dataset.py:398:                [int(w[0] / self.clip_len), min(int(w[1] / self.clip_len), ctx_l) - 1]
cg_detr/start_end_dataset.py:462:            min_len = min([len(e) for e in v_feat_list])
cg_detr/start_end_dataset.py:463:            v_feat_list = [e[:min_len] for e in v_feat_list]
cg_detr/start_end_dataset.py:482:            min_len = min([len(e) for e in v_feat_list])
cg_detr/start_end_dataset.py:483:            v_feat_list = [e[:min_len] for e in v_feat_list] # TODO do we need to cut the length over the min_len?
cg_detr/start_end_dataset.py:499:            min_len = min([len(e) for e in v_feat_list])
cg_detr/start_end_dataset.py:500:            v_feat_list = [e[:min_len] for e in v_feat_list]
cg_detr/model_otmi.py:3:sys.path.insert(0, "/mnt/DATA/hari/vand/VTG_Project/CGDETR_OTMI")
cg_detr/model_otmi.py:8:class MIQueryInit(nn.Module):
cg_detr/model_otmi.py:19:    def mi_loss(self, q, vg):
cg_detr/model_otmi.py:38:        return q, self.mi_loss(q, vg), self.div_loss(q)
cg_detr/model_otmi.py:45:    # Attach MI query module
cg_detr/model_otmi.py:48:    model.mi_query = MIQueryInit(d, nq=nq)
cg_detr/model_otmi.py:50:    # Wrap forward to add MI loss
cg_detr/model.py:14:from cg_detr.misc import accuracy
cg_detr/model.py:19:    x = x.clamp(min=0, max=1)
cg_detr/model.py:20:    x1 = x.clamp(min=eps)
cg_detr/model.py:21:    x2 = (1 - x).clamp(min=eps)
cg_detr/model.py:58:                 contrastive_align_loss=False, contrastive_hdim=64,
cg_detr/model.py:70:            contrastive_align_loss: If true, perform span - tokens contrastive learning
cg_detr/model.py:71:            contrastive_hdim: dimension used for projecting the embeddings before computing contrastive loss
cg_detr/model.py:108:        self.contrastive_align_loss = contrastive_align_loss
cg_detr/model.py:109:        if contrastive_align_loss:
cg_detr/model.py:110:            self.contrastive_align_projection_query = nn.Linear(hidden_dim, contrastive_hdim)
cg_detr/model.py:111:            self.contrastive_align_projection_txt = nn.Linear(hidden_dim, contrastive_hdim)
cg_detr/model.py:112:            self.contrastive_align_projection_vid = nn.Linear(hidden_dim, contrastive_hdim)
cg_detr/model.py:123:        self.dummy_rep_token = torch.nn.Parameter(torch.randn(args.num_dummies, hidden_dim))
cg_detr/model.py:124:        self.dummy_rep_pos = torch.nn.Parameter(torch.randn(args.num_dummies, hidden_dim))
cg_detr/model.py:152:                               See PostProcess for information on how to retrieve the unnormalized bounding box.
cg_detr/model.py:176:        txt_dummy = self.dummy_rep_token.reshape([1, self.args.num_dummies, self.hidden_dim]).repeat(src_txt.shape[0], 1, 1)
cg_detr/model.py:178:        mask_txt = torch.tensor([[True] * self.args.num_dummies]).to(src_txt_mask.device).repeat(src_txt_mask.shape[0], 1)
cg_detr/model.py:181:        pos_dummy = self.dummy_rep_pos.reshape([1, self.args.num_dummies, self.hidden_dim]).repeat(pos_txt.shape[0], 1, 1)
cg_detr/model.py:187:        dummy_token = memory[:self.args.num_dummies].permute(1, 0, 2)
cg_detr/model.py:191:        mask_txt_dummy = torch.tensor([[True]*self.args.num_dummies]).to(src_txt_mask.device).repeat(src_txt_mask.shape[0], 1)
cg_detr/model.py:263:            moment2txt_similarity = torch.matmul(mmemory_frames.permute(1, 0, 2), txt_dummy_proj.permute(1, 2, 0))
cg_detr/model.py:264:            nmoment2txt_similarity = torch.matmul(nmmemory_frames.permute(1, 0, 2), txt_dummy_proj.permute(1, 2, 0))
cg_detr/model.py:266:            sentence_dummy, sentence_txt, moment2txt_similarity, nmoment2txt_similarity = None, None, None, None
cg_detr/model.py:279:        if self.contrastive_align_loss:
cg_detr/model.py:280:            proj_queries = F.normalize(self.contrastive_align_projection_query(hs), p=2, dim=-1)
cg_detr/model.py:281:            proj_txt_mem = F.normalize(self.contrastive_align_projection_txt(txt_mem), p=2, dim=-1)
cg_detr/model.py:282:            proj_vid_mem = F.normalize(self.contrastive_align_projection_vid(vid_mem), p=2, dim=-1)
cg_detr/model.py:313:                out["t2vattnvalues_neg"] = (attn_weights_neg[:, :, self.args.num_dummies:] * (src_txt_mask_dummy_neg[:, self.args.num_dummies:].unsqueeze(1).repeat(1, video_length, 1))).sum(2)
cg_detr/model.py:332:        out["moment2txt_similarity"] = moment2txt_similarity
cg_detr/model.py:333:        out["nmoment2txt_similarity"] = nmoment2txt_similarity
cg_detr/model.py:339:        out["t2vattnvalues"] = (attn_weights[:,:,self.args.num_dummies:] * (src_txt_mask.unsqueeze(1).repeat(1, video_length, 1))).sum(2) # (batch_size, L_vid, L_txt) / (batch_size, L_txt)
cg_detr/model.py:355:            if self.contrastive_align_loss:
cg_detr/model.py:402:        # moment sentence contrastive
cg_detr/model.py:405:        self.kld_criterion = torch.nn.KLDivLoss(reduction='none').to(self.args.device)
cg_detr/model.py:466:            saliency_contrast_label = targets["saliency_all_labels"]
cg_detr/model.py:470:            realneg_saliency_contrast_label = torch.cat([saliency_contrast_label[real_neg_mask], torch.zeros_like(saliency_contrast_label)[real_neg_mask]], dim=1)
cg_detr/model.py:475:            loss_rank_contrastive = 0.
cg_detr/model.py:477:                drop_mask = ~(realneg_saliency_contrast_label > 100)  # no drop
cg_detr/model.py:478:                pos_mask = (realneg_saliency_contrast_label >= rand_idx)  # positive when equal or higher than rand_idx
cg_detr/model.py:494:                loss_rank_contrastive = loss_rank_contrastive + loss.mean()
cg_detr/model.py:495:            loss_rank_contrastive = loss_rank_contrastive / 12
cg_detr/model.py:501:                    falseneg_saliency_contrast_label = saliency_contrast_label[false_neg_mask].unsqueeze(0)
cg_detr/model.py:506:                    falseneg_saliency_contrast_label = saliency_contrast_label[false_neg_mask]
cg_detr/model.py:511:                falseneg_loss_rank_contrastive = 0.
cg_detr/model.py:513:                    drop_mask = ~(falseneg_saliency_contrast_label > 100)  # no drop
cg_detr/model.py:514:                    pos_mask = (falseneg_saliency_contrast_label >= rand_idx)  # positive when equal or higher than rand_idx
cg_detr/model.py:530:                    falseneg_loss_rank_contrastive = falseneg_loss_rank_contrastive + loss.mean()
cg_detr/model.py:531:                falseneg_loss_rank_contrastive = falseneg_loss_rank_contrastive / 12
cg_detr/model.py:532:                loss_rank_contrastive += falseneg_loss_rank_contrastive
cg_detr/model.py:543:            loss_saliency = torch.clamp(self.saliency_margin + neg_scores - pos_scores, min=0).sum() \
cg_detr/model.py:547:                loss_saliency = loss_saliency + loss_rank_contrastive + loss_neg_pair * 0.
cg_detr/model.py:549:                loss_saliency = loss_saliency + loss_rank_contrastive + loss_neg_pair
cg_detr/model.py:561:            saliency_contrast_label = targets["saliency_all_labels"]
cg_detr/model.py:565:            realneg_saliency_contrast_label = torch.cat(
cg_detr/model.py:566:                [saliency_contrast_label[real_neg_mask], torch.zeros_like(saliency_contrast_label)[real_neg_mask]], dim=1)
cg_detr/model.py:572:            loss_rank_contrastive_attn = 0.
cg_detr/model.py:574:                drop_mask = ~(realneg_saliency_contrast_label > 100)  # no drop
cg_detr/model.py:575:                pos_mask = (realneg_saliency_contrast_label >= rand_idx)  # positive when equal or higher than rand_idx
cg_detr/model.py:591:                loss_rank_contrastive_attn = loss_rank_contrastive_attn + loss.mean()
cg_detr/model.py:592:            loss_rank_contrastive_attn = loss_rank_contrastive_attn / 12
cg_detr/model.py:598:                    falseneg_saliency_contrast_label = saliency_contrast_label[false_neg_mask].unsqueeze(0)
cg_detr/model.py:603:                    falseneg_saliency_contrast_label = saliency_contrast_label[false_neg_mask]
cg_detr/model.py:608:                falseneg_loss_rank_contrastive = 0.
cg_detr/model.py:610:                    drop_mask = ~(falseneg_saliency_contrast_label > 100)  # no drop
cg_detr/model.py:611:                    pos_mask = (falseneg_saliency_contrast_label >= rand_idx)  # positive when equal or higher than rand_idx
cg_detr/model.py:627:                    falseneg_loss_rank_contrastive = falseneg_loss_rank_contrastive + loss.mean()
cg_detr/model.py:628:                falseneg_loss_rank_contrastive = falseneg_loss_rank_contrastive / 12
cg_detr/model.py:629:                loss_rank_contrastive += falseneg_loss_rank_contrastive
cg_detr/model.py:640:            loss_saliency_attn = torch.clamp(self.saliency_margin + neg_scores - pos_scores, min=0).sum() \
cg_detr/model.py:650:                loss_saliency_attn = loss_rank_contrastive_attn + bceloss + loss_neg_pair_attn * 0 + loss_saliency_attn
cg_detr/model.py:652:                loss_saliency_attn = loss_rank_contrastive_attn + bceloss + loss_neg_pair_attn + loss_saliency_attn
cg_detr/model.py:658:            saliency_contrast_label = targets["saliency_all_labels"]
cg_detr/model.py:663:            loss_rank_contrastive = 0.
cg_detr/model.py:665:                drop_mask = ~(saliency_contrast_label > 100)  # no drop
cg_detr/model.py:666:                pos_mask = (saliency_contrast_label >= rand_idx)  # positive when equal or higher than rand_idx
cg_detr/model.py:682:                loss_rank_contrastive = loss_rank_contrastive + loss.mean()
cg_detr/model.py:683:            loss_rank_contrastive = loss_rank_contrastive / 12
cg_detr/model.py:694:            loss_saliency = torch.clamp(self.saliency_margin + neg_scores - pos_scores, min=0).sum() \
cg_detr/model.py:697:            loss_saliency = loss_saliency + loss_rank_contrastive
cg_detr/model.py:702:            saliency_contrast_label = targets["saliency_all_labels"]
cg_detr/model.py:707:            loss_rank_contrastive = 0.
cg_detr/model.py:709:                drop_mask = ~(saliency_contrast_label > 100)  # no drop
cg_detr/model.py:710:                pos_mask = (saliency_contrast_label >= rand_idx)  # positive when equal or higher than rand_idx
cg_detr/model.py:726:                loss_rank_contrastive = loss_rank_contrastive + loss.mean()
cg_detr/model.py:727:            loss_rank_contrastive_attn = loss_rank_contrastive / 12
cg_detr/model.py:738:            loss_saliency_attn = torch.clamp(self.saliency_margin + neg_scores - pos_scores, min=0).sum() \
cg_detr/model.py:746:            loss_saliency_attn = loss_rank_contrastive_attn + bceloss + loss_saliency_attn
cg_detr/model.py:750:    def loss_contrastive_moment_sentence(self, outputs, targets, indices, log=True):
cg_detr/model.py:762:            similarity_matrix = torch.matmul(moment_logits, sentence_logits.T) # B B
cg_detr/model.py:763:            nsimilarity_matrix = torch.matmul(nmoment_logits, sentence_logits.T) # B B
cg_detr/model.py:764:            similarity_matrix = torch.cat([similarity_matrix, nsimilarity_matrix], dim=1)
cg_detr/model.py:765:            labels = torch.eye(similarity_matrix.shape[0]).to(self.args.device)
cg_detr/model.py:766:            nlabels = torch.zeros_like(nsimilarity_matrix).to(self.args.device)
cg_detr/model.py:769:            loss_ms_align = self.criterion(similarity_matrix, labels)
cg_detr/model.py:771:            dummy_similarity_matrix = torch.matmul(moment_logits, dummy_logits.T)
cg_detr/model.py:772:            dummy_nsimilarity_matrix = torch.matmul(nmoment_logits, dummy_logits.T)
cg_detr/model.py:773:            dummy_similarity_matrix = torch.cat([dummy_similarity_matrix, dummy_nsimilarity_matrix], dim=1)
cg_detr/model.py:774:            dummy_labels = (~(torch.eye(similarity_matrix.shape[0]).to(self.args.device).bool())).float()
cg_detr/model.py:775:            dummy_nlabels = torch.ones_like(nsimilarity_matrix).to(self.args.device)
cg_detr/model.py:778:            dummy_loss_ms_align = self.criterion(dummy_similarity_matrix, dummy_labels)
cg_detr/model.py:795:        if outputs["moment2txt_similarity"] is not None:
cg_detr/model.py:796:            moment2txt_similarity = outputs["moment2txt_similarity"]  # bsz L_clip 22
cg_detr/model.py:802:            loss_distill = self.kld_criterion(
cg_detr/model.py:804:                torch.softmax(moment2txt_similarity, dim=-1).clone().detach().reshape(b * L_vid, -1)).mean(1) * moment_mask.reshape(-1)
cg_detr/model.py:831:    def loss_contrastive_align(self, outputs, targets, indices, log=True):
cg_detr/model.py:847:        losses = {"loss_contrastive_align": loss_nce.mean()}
cg_detr/model.py:850:    def loss_contrastive_align_vid_txt(self, outputs, targets, indices, log=True):
cg_detr/model.py:866:        losses = {"loss_contrastive_align": loss_nce.mean()}
cg_detr/model.py:885:            "contrastive_align": self.loss_contrastive_align,
cg_detr/model.py:887:            "ms_align": self.loss_contrastive_moment_sentence,
cg_detr/model.py:998:            contrastive_align_loss=args.contrastive_align_loss,
cg_detr/model.py:999:            contrastive_hdim=args.contrastive_hdim,
cg_detr/model.py:1016:            contrastive_align_loss=args.contrastive_align_loss,
cg_detr/model.py:1017:            contrastive_hdim=args.contrastive_hdim,
cg_detr/model.py:1032:    if args.contrastive_align_loss:
cg_detr/model.py:1033:        weight_dict["loss_contrastive_align"] = args.contrastive_align_loss_coef
cg_detr/model.py:1042:    if args.contrastive_align_loss:
cg_detr/model.py:1043:        losses += ["contrastive_align"]
cg_detr/inference.py:14:from cg_detr.model_otmi import build_model
cg_detr/inference.py:18:from standalone_eval.eval import eval_submission
cg_detr/inference.py:27:                    level=logging.INFO)
cg_detr/inference.py:42:def eval_epoch_post_processing(submission, opt, gt_data, save_submission_filename):
cg_detr/inference.py:44:    logger.info("Saving/Evaluating before nms results")
cg_detr/inference.py:45:    submission_path = os.path.join(opt.results_dir, save_submission_filename)
cg_detr/inference.py:46:    save_jsonl(submission, submission_path)
cg_detr/inference.py:49:        metrics = eval_submission(
cg_detr/inference.py:50:            submission, gt_data,
cg_detr/inference.py:53:        save_metrics_path = submission_path.replace(".jsonl", "_metrics.json")
cg_detr/inference.py:55:        latest_file_paths = [submission_path, save_metrics_path]
cg_detr/inference.py:58:        latest_file_paths = [submission_path, ]
cg_detr/inference.py:61:        logger.info("[MR] Performing nms with nms_thd {}".format(opt.nms_thd))
cg_detr/inference.py:62:        submission_after_nms = post_processing_mr_nms(
cg_detr/inference.py:63:            submission, nms_thd=opt.nms_thd,
cg_detr/inference.py:67:        logger.info("Saving/Evaluating nms results")
cg_detr/inference.py:68:        submission_nms_path = submission_path.replace(".jsonl", "_nms_thd_{}.jsonl".format(opt.nms_thd))
cg_detr/inference.py:69:        save_jsonl(submission_after_nms, submission_nms_path)
cg_detr/inference.py:71:            metrics_nms = eval_submission(
cg_detr/inference.py:72:                submission_after_nms, gt_data,
cg_detr/inference.py:75:            save_metrics_nms_path = submission_nms_path.replace(".jsonl", "_metrics.json")
cg_detr/inference.py:77:            latest_file_paths += [submission_nms_path, save_metrics_nms_path]
cg_detr/inference.py:80:            latest_file_paths = [submission_nms_path, ]
cg_detr/inference.py:206:    submmission = dict(mAP=round(mean_ap, 5))
cg_detr/inference.py:214:    return submmission, loss_meters 
cg_detr/inference.py:288:            clip_length=opt.clip_length, min_ts_val=0, max_ts_val=150,
cg_detr/inference.py:289:            min_w_l=2, max_w_l=150, move_window_method="left",
cg_detr/inference.py:295:                clip_length=opt.clip_length, min_ts_val=0, max_ts_val=360,
cg_detr/inference.py:296:                min_w_l=12, max_w_l=360, move_window_method="left",
cg_detr/inference.py:301:                clip_length=opt.clip_length, min_ts_val=0, max_ts_val=150,
cg_detr/inference.py:302:                min_w_l=2, max_w_l=60, move_window_method="left",
cg_detr/inference.py:307:            clip_length=opt.clip_length, min_ts_val=0, max_ts_val=50000,
cg_detr/inference.py:308:            min_w_l=0, max_w_l=50000, move_window_method="left",
cg_detr/inference.py:322:def eval_epoch(model, eval_dataset, opt, save_submission_filename, epoch_i=None, criterion=None, tb_writer=None):
cg_detr/inference.py:323:    logger.info("Generate submissions")
cg_detr/inference.py:350:        submission = [
cg_detr/inference.py:353:        submission_path = os.path.join(opt.results_dir, "latest_metric.jsonl")
cg_detr/inference.py:354:        save_jsonl(submission, submission_path)
cg_detr/inference.py:356:        return submission[0], submission[0], eval_loss_meters, [submission_path]
cg_detr/inference.py:359:        submission, eval_loss_meters = get_eval_res(model, eval_loader, opt, epoch_i, criterion, tb_writer)
cg_detr/inference.py:362:            new_submission = []
cg_detr/inference.py:363:            for s in submission:
cg_detr/inference.py:365:                new_submission.append(s)
cg_detr/inference.py:366:            submission = new_submission
cg_detr/inference.py:369:            save_submission_filename = save_submission_filename.replace(".jsonl", "_unsorted.jsonl")
cg_detr/inference.py:371:            submission, opt, eval_dataset.data, save_submission_filename)
cg_detr/inference.py:376:    """setup model/optimizer/scheduler and load checkpoints when needed"""
cg_detr/inference.py:377:    logger.info("setup model/optimizer/scheduler")
cg_detr/inference.py:380:        logger.info("CUDA enabled.")
cg_detr/inference.py:385:    optimizer = torch.optim.AdamW(param_dicts, lr=opt.lr, weight_decay=opt.wd)
cg_detr/inference.py:386:    lr_scheduler = torch.optim.lr_scheduler.StepLR(optimizer, opt.lr_drop)
cg_detr/inference.py:389:        logger.info(f"Load checkpoint from {opt.resume}")
cg_detr/inference.py:405:            optimizer.load_state_dict(checkpoint['optimizer'])
cg_detr/inference.py:408:        logger.info(f"Loaded model saved at epoch {checkpoint['epoch']} from checkpoint: {opt.resume}")
cg_detr/inference.py:412:    return model, criterion, optimizer, lr_scheduler
cg_detr/inference.py:427:    logger.info("Setup config, data and model...")
cg_detr/inference.py:431:    cudnn.deterministic = False
cg_detr/inference.py:463:    save_submission_filename = "hl_{}_submission.jsonl".format(
cg_detr/inference.py:465:    # save_submission_filename = "inference_{}_{}_{}_preds.jsonl".format(
cg_detr/inference.py:467:    logger.info("Starting inference...")
cg_detr/inference.py:470:            eval_epoch(model, eval_dataset, opt, save_submission_filename, criterion=criterion)
cg_detr/inference.py:472:        logger.info("metrics_no_nms {}".format(pprint.pformat(metrics_no_nms["brief"], indent=4)))
cg_detr/inference.py:474:        logger.info("metrics_nms {}".format(pprint.pformat(metrics_nms["brief"], indent=4)))
cg_detr/transformer.py:35:    x = x.clamp(min=0, max=1)
cg_detr/transformer.py:36:    x1 = x.clamp(min=eps)
cg_detr/transformer.py:37:    x2 = (1 - x).clamp(min=eps)
cg_detr/transformer.py:76:                                                dropout, activation, normalize_before, self.args.num_dummies)
cg_detr/transformer.py:151:        ### Video Clip featre - context (avg) --> Find top 10 similar tokens --> weighted sum
cg_detr/transformer.py:154:        frame_importance = attn_weights[:, :, self.args.num_dummies:].sum(2).clone().detach()  # b 75
cg_detr/transformer.py:160:        ### Scale the similarity with importance
cg_detr/transformer.py:471:                 activation="relu", normalize_before=False, num_dummies=3):
cg_detr/transformer.py:473:        self.self_attn = cateattention(d_model, nhead, dropout=dropout, num_dummies=num_dummies)
cg_detr/config.py:53:        parser.add_argument("--bsz", type=int, default=32, help="mini-batch size")
cg_detr/config.py:55:                            help="mini-batch size at inference, for query")
cg_detr/config.py:63:                            help="if --resume_all, load optimizer/scheduler/epoch as well")
cg_detr/config.py:119:        parser.add_argument('--num_dummies', default=45, type=int,
cg_detr/config.py:128:        parser.add_argument("--contrastive_hdim", type=int, default=64, help="dim for contrastive embeddings")
cg_detr/config.py:129:        parser.add_argument("--temperature", type=float, default=0.07, help="temperature nce contrastive_align_loss")
cg_detr/config.py:137:        parser.add_argument("--contrastive_align_loss", action="store_true",
cg_detr/config.py:138:                            help="Disable contrastive_align_loss between matched query spans and the text.")
cg_detr/config.py:161:        parser.add_argument("--contrastive_align_loss_coef", default=0.0, type=float)
cg_detr/config.py:170:                                 "(or non-minimum suppression for distance)"
cg_detr/config.py:202:                               "max_pred_l", "min_pred_l",
cg_detr/config.py:216:                                                     'ndum_' + str(opt.num_dummies), 'nprom_' + str(opt.num_prompts) + '_' + str(opt.total_prompts)]))
cg_detr/attention.py:7:# Copyright (c) 2021 Microsoft. All Rights Reserved.
cg_detr/attention.py:62:    r"""Allows the model to jointly attend to information
cg_detr/position_encoding.py:37:    This is a more standard version of the position embedding, very similar to the one
cg_detr/crossattention.py:7:# Copyright (c) 2021 Microsoft. All Rights Reserved.
cg_detr/crossattention.py:62:    r"""Allows the model to jointly attend to information
cg_detr/crossattention.py:87:    def __init__(self, embed_dim, num_heads, dropout=0., num_dummies=3, bias=True, add_bias_kv=False, add_zero_attn=False, kdim=None, vdim=None):
cg_detr/crossattention.py:89:        self.num_dummies = num_dummies
cg_detr/crossattention.py:174:                v_proj_weight=self.v_proj_weight, out_dim=self.vdim, num_dummies=self.num_dummies, dummy=dummy)
cg_detr/crossattention.py:183:                attn_mask=attn_mask, out_dim=self.vdim, num_dummies=self.num_dummies, dummy=dummy)
cg_detr/crossattention.py:210:                                 num_dummies=3,
cg_detr/crossattention.py:384:        attn_output = torch.bmm(attn_output_weights_d[:, :, num_dummies:], v[:, num_dummies:,:])
cg_detr/span_utils.py:65:    right = torch.min(spans1[:, None, 1], spans2[:, 1])  # (N, M)
cg_detr/span_utils.py:67:    inter = (right - left).clamp(min=0)  # (N, M)
cg_detr/span_utils.py:84:    right = torch.min(gt_spans[:, None, 1], pred_spans[:, 1])
cg_detr/span_utils.py:86:    inter = (right - left).clamp(min=0)  # (N, M)
cg_detr/span_utils.py:116:    left = torch.min(spans1[:, None, 0], spans2[:, 0])  # (N, M)
cg_detr/span_utils.py:118:    enclosing_area = (right - left).clamp(min=0)  # (N, M)
